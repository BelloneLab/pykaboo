# Python dependencies for CamApp Live Detection
# Install with: pip install -r requirements.txt
#
# Note: FFmpeg is used by the app for recording, but it is not a Python
# package. Install FFmpeg separately and make sure it is available on PATH.
# Note: FLIR Spinnaker support uses the vendor-provided PySpin wheel from the
# Spinnaker SDK and is not installed from PyPI here.

PySide6>=6.6.0
numpy>=1.24,<2
scipy
Pillow
torch
torchvision
rfdetr
ultralytics
pyqtgraph>=0.13.0
pypylon>=2.0.0
flirpy>=0.4.0
opencv-python>=4.8,<4.12
pandas>=2.0.0
pyserial
pyfirmata>=1.1.0
matplotlib
